import 'package:flutter/material.dart';

class HomeViewTypeFoods extends StatelessWidget {
  const HomeViewTypeFoods({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('HomeViewTypeFoods'),
    );
  }
}
